create database StudentsDB;
use StudentsDB;
create table students(Student_ID int primary key,
					First_name text,
                    last_name text,
                    Date_of_birth date,
                    Email text,
                    Major text);
select * from students;
                    

